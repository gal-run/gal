# v0.3.2

Renames Stratus provider metadata from capacity providers to GPU providers.

## Included

- provider integration profiles point to `@stratus/gpu-provider-*`
- README names concrete provisioning as Stratus GPU providers
- clarifies that the concrete provider surface is GPU-specific, not generic capacity or compute

## Validation

- `npm install --package-lock-only`
- `npm run type-check`
- `npm test`
- `npm run build`
- `npm run smoke:consumer`
- `npm pack --pack-destination /tmp`

Artifact SHA-256:

```text
c43adb60963dcefec1bd39c1f3e15b8483f15def078be274297dcab81cb13bd1  gal-swarm-0.3.2.tgz
```
