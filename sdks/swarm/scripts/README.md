# Scripts

This folder is for local validation harnesses, not runtime product logic.

- `smoke/` contains packaging and consumer smoke tests.
- `proofs/` contains evidence-producing proof harnesses for documented control-plane or SLO claims.

Prefer package scripts in `package.json` as the stable entrypoints.
