# react-diff-viewer-continued@3.4.0 declares a React <=18 peer range but works
# with React 19 (used by apps/dashboard). pnpm tolerated this; npm needs the
# flag to install. Keep here so `npm install`/CI resolve without manual flags.
legacy-peer-deps=true
