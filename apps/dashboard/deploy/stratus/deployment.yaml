# WARNING: NOT THE SOURCE OF TRUTH
#
# The live deployment manifest is managed by ArgoCD at:
#   StratusCloudLabs/argocdgitops → clusters/stratus/workloads/gal-dashboard/deployment.yaml
#
# The image digest in that manifest is updated automatically by CI on every push to main.
# Do NOT edit this file for production changes — edits here have no effect on the cluster.
#
# For local dev / reference shape, see deploy/stratus/README.md.
