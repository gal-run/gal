{
  "manifest_version": 3,
  "name": "gal",
  "version": "0.0.1",
  "description": "gal — gal-chrome: drives the REAL Chrome via chrome.debugger (CDP) + chrome.tabs/tabGroups, controlled over a local WebSocket by the gal-chrome MCP server. The chrome.debugger banner reads 'gal started debugging this browser'.",
  "permissions": ["debugger", "tabs", "tabGroups", "alarms"],
  "host_permissions": ["<all_urls>"],
  "background": { "service_worker": "background.js" }
}
